package com.example.util;

public interface StringUtil {

    /**
     * @return true if the String is null, empty or contains only whitespace, else false.
     * */
    boolean isBlank(String string);
}